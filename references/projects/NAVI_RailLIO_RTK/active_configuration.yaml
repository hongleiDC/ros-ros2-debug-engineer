project: NAVI_RailLIO_RTK
status: measured
ros:
  ros1_distribution: noetic
  ros2_distribution: humble
frontend:
  algorithm: ct-lio-eskf
  lidar_topic_ros1: /lidar
  lidar_topic_ros2: /lidar_time
  imu_topic: /handsfree/imu
  odom_topic: /odom
  use_fixed_extrinsic: true
  imu_acc_scale: 1.0
  delay_time_sec: 0.0
  time_offset_lidar_to_imu_sec:
    value: 0.0
    status: candidate
    sign: t_lidar_corrected = t_lidar_raw + offset
frames:
  map: map
  base: base_link
  imu: imu_link
  lidar: lidar_link
  rtk_enu: rtk_enu
notes:
  - verify actual frame ids from runtime tf before code changes
  - do not publish duplicate static tf during bag replay
